import type { BracketTree } from "../domain"

export class BracketLoader {
  static load(
    rows: unknown[],
  ): BracketTree {
    throw new Error(
      "BracketLoader not implemented.",
    )
  }
}